Торяшиев Жаргал Б763-2а


7. Как записываются литералы различных видов и типов в Java-программе?
int - целочисленные
float - вещественные
String - сторки

Вопрос 2.1. 
Какое значение примет переменная value в результате выполнения фрагмен- 
та кода? (выбрать один) 
boolean flag = true; 
int value; 
if (flag) {
value = flag ? 1 : 2; 
} else { 
value = flag ? 3 : 4; 
} 
Ответ: a

Вопрос 2.2. 
Какое значение будет выведено в консоль в результате выполнения цикла? 
(выберите один) 
for (int i = 0, j = 0; i < 3 && j <= 0; i++, j--) { 
if (i != j) { 
continue; 
} 
System.out.print (i + " " + j); 
}
Ответ: a

Вопрос 2.3. 
Какое из следующих объявлений массива корректно? (выбрать один) 
Ответ: d

Вопрос 2.4. 
Что будет результатом компиляции и запуска следующего кода? (выбрать 
один) 
public class Quest { 
public static void main(String args[]) { 
char[] array = {'a', 'b', 'c'}; 
for (char element : array) { 
element += element; 
} 
System.out.println(Arrays.toString(array)); 
} 
}
Ответ: e

Вопрос 2.5. 
Даны объявления строк: 
String s1 = new String("Java12"); 
String s2 = new String("12"); 
String s3 = new String(); 
Ответ: a

Вопрос 2.6. 
Какие из ключевых слов могут быть использованы при объявлении метода 
класса? (выбрать два) 
Ответ: a c

Вопрос 2.7. 
Какие из следующих имен переменных корректны? (выбрать три)
Ответ: a b c

Вопрос 2.8. 
Какое из предложенных значений представляет значение по умолчанию для 
типа boolean? (выбрать один)
Ответ: b
